spring:
  profiles:
    include: oauth2
  config:
    import:
      - optional:file:secret.env[.properties]
      - classpath:/dev/secret.yml
      - classpath:/dev/basic.yml
springdoc:
  swagger-ui:
    path: /api-test  # swagger-ui ?? ??? ?? ??, ?? ??? ???? http://localhost:8080/swagger-ui/index.html? ????? ?.
    groups-order: DESC # path, query, body, response ??? ??
    tags-sorter: alpha # ??? ??? ??? ??
    operations-sorter: method  # delete - get - patch - post - put ??? ??, alpha? ???? ??? ??? ?? ??
  paths-to-match: /api/** # swagger-ui? ??? global_api? ?????  ?





